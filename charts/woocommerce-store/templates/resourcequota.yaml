apiVersion: v1
kind: ResourceQuota
metadata:
  name: woo-store-quota
  namespace: woo-store
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "5"
    pods: "10"
